# Application name
spring.application.name=netflix-api

# Server configuration
server.port=8080

# Logging level (optional but helpful)
logging.level.org.springframework=INFO

# JSON formatting (optional)
spring.jackson.serialization.indent_output=true

